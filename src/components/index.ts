// 导出所有组件
import Button from './button';
import Input from './input';

// 导出所有类型
import type { ButtonProps, ButtonPropsWithEl } from './button';
import type { InputProps, InputPropsWithEl } from './input';

export {
  Button,
  Input
}

export type {
  ButtonProps,
  ButtonPropsWithEl,
  InputProps,
  InputPropsWithEl
}

export default {
  Button,
  Input
}